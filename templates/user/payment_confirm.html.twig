{% extends 'base.html.twig' %}

{% block title %}Paiement confirmé{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card text-center">
                <div class="card-body p-5">
                    <div class="mb-4">
                        <svg xmlns="http://www.w3.org/2000/svg" width="100" height="100" fill="currentColor" 
                             class="bi bi-check-circle-fill text-success" viewBox="0 0 16 16">
                            <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zm-3.97-3.03a.75.75 0 0 0-1.08.022L7.477 9.417 5.384 7.323a.75.75 0 0 0-1.06 1.06L6.97 11.03a.75.75 0 0 0 1.079-.02l3.992-4.99a.75.75 0 0 0-.01-1.05z"/>
                        </svg>
                    </div>
                    
                    <h1 class="text-success mb-4">Paiement confirmé !</h1>
                    
                    <p class="lead mb-4">
                        Merci pour votre commande, <strong>{{ app.user.prenom }} {{ app.user.nom }}</strong> !
                    </p>
                    
                    <div class="alert alert-info" role="alert">
                        <strong>Numéro de commande :</strong> #{{ random(10000, 99999) }}
                    </div>
                    
                    <p class="mb-4">
                        Un email de confirmation vous a été envoyé à l'adresse : 
                        <strong>{{ app.user.email }}</strong>
                    </p>
                    
                    <div class="card mb-4">
                        <div class="card-header bg-light">
                            <h5 class="mb-0"> Informations de livraison</h5>
                        </div>
                        <div class="card-body text-start">
                            <p class="mb-1"><strong>Délai de livraison estimé :</strong> 3-5 jours ouvrés</p>
                            <p class="mb-1"><strong>Mode de livraison :</strong> Standard (Gratuit)</p>
                            <p class="mb-0"><strong>Suivi :</strong> Vous recevrez un numéro de suivi par email</p>
                        </div>
                    </div>
                    
                    <div class="d-grid gap-2">
                        <a href="{{ path('app_products') }}" class="btn btn-primary btn-lg">
                            Continuer mes achats
                        </a>
                        <a href="{{ path('app_user_show', {'id': app.user.id}) }}" class="btn btn-outline-secondary">
                            Voir mon profil
                        </a>
                        <a href="{{ path('app_home') }}" class="btn btn-outline-secondary">
                            Retour à l'accueil
                        </a>
                    </div>
                </div>
            </div>
            
            <div class="card mt-4">
                <div class="card-body">
                    <h5 class="card-title">Besoin d'aide ?</h5>
                    <p class="card-text">
                        Si vous avez des questions concernant votre commande, n'hésitez pas à contacter notre service client.
                    </p>
                    <a href="{{ path('app_support') }}" class="btn btn-outline-primary">
                        Contacter le support
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
